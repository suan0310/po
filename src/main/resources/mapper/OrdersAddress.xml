<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bebe.spring.order.dao.OrdersDAO">
<!-- 	<insert id="insertOrders"   parameterType="int"> -->
<!-- 		insert all  -->
<!-- 		into orders values (or_no.nextval,'okkk',#{productNo},#{orderQty},#{orderPrice},'배송준비','취소') -->
<!-- 		into address values('okkk',or_no.nextval ,#{zipcode},#{addr},#{addrDetail},#{tel},#{addrReceiver}, #{addrRequest}) -->
<!-- 		select * from dual -->
<!-- 	</insert> -->
			<insert id="insertOrders" parameterType="list">
				<foreach collection="list" item="i" separator=" " open="Insert all" close="select * from dual">
					into orders values (or_no.nextval,'okkk',#{i.productNo},#{i.orderQty},#{i.orderPrice},'배송준비','취소') 
					into address values('okkk',or_no.nextval ,#{i.zipcode},#{i.addr},#{i.addrDetail},#{i.tel},#{i.addrReceiver}, #{i.addrRequest}) 
	
				</foreach>
			</insert>
		
</mapper>